# Development + test dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest~=8.2
pytest-asyncio~=0.23
pytest-cov~=5.0
respx~=0.21          # httpx request mocking for offline service tests
freezegun~=1.5       # deterministic time for recency + cooldown tests
